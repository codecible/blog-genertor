# OpenAI API配置
OPENAI_API_KEY=your_api_key_here

# API请求配置（可选）
OPENAI_MODEL=gpt-3.5-turbo # 使用的模型（可选，默认为gpt-3.5-turbo）
MAX_TOKENS=1000 # 最大生成token数（可选，默认为1000）
TEMPERATURE=0.7 # 生成文本的创造性程度（可选，默认为0.7）
